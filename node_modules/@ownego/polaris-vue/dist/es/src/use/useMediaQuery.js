import { ref } from "vue";
import { d as debounce } from "../../polaris/polaris-react/src/utilities/debounce.js";
import { n as navigationBarCollapsed } from "./useBreakpoints.js";
import { u as useEventListener } from "../utilities/use-event-listener.js";
function useMediaQueryContext() {
  const isNavigationCollapsed = ref(navigationBarCollapsed().matches);
  const handleResize = debounce(
    () => {
      if (isNavigationCollapsed.value !== navigationBarCollapsed().matches) {
        isNavigationCollapsed.value = !isNavigationCollapsed.value;
      }
    },
    40,
    { trailing: true, leading: true, maxWait: 40 }
  );
  useEventListener("resize", handleResize);
  return {
    isNavigationCollapsed
  };
}
export {
  useMediaQueryContext as u
};
//# sourceMappingURL=useMediaQuery.js.map
