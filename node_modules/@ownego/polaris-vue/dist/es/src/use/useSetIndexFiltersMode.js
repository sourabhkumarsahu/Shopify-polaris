import { ref } from "vue";
import { I as IndexFiltersMode } from "../components/IndexFilters/types.js";
function useSetIndexFiltersMode(defaultMode = IndexFiltersMode.Default) {
  const mode = ref(defaultMode);
  const setMode = (newMode) => {
    mode.value = newMode;
  };
  return { mode, setMode };
}
export {
  useSetIndexFiltersMode as u
};
//# sourceMappingURL=useSetIndexFiltersMode.js.map
